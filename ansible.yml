config:
  test_groups:
    - cenv
    - core

provision:
  cenv-info:
    module: kube
    task: info
    servers: groups.name.in=@test_groups

  server-list:
    requires: cenv-info
    command: server list
