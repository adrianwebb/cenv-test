parents:
  - module: cluster
    profile: aws-cenv

  - module: cluster
    profile: aws-storage

  # - module: kube
  #   profile: aws

config:
  aws_networks:
    - aws-us-east-1
    - aws-us-east-2
